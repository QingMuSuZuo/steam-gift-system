<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>提货成功 - Steam礼物系统</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/public.css">
    <style>
        .success-container {
            max-width: 600px;
            margin: 50px auto;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            background-color: #fff;
        }
        
        .success-icon {
            font-size: 80px;
            color: #28a745;
            margin-bottom: 20px;
        }
        
        .game-image {
            max-width: 200px;
            border-radius: 5px;
            margin: 20px 0;
        }
        
        .steps-container {
            text-align: left;
            margin: 30px 0;
            padding: 15px;
            background-color: #f8f9fa;
            border-radius: 8px;
        }
        
        .step {
            margin-bottom: 15px;
            display: flex;
            align-items: flex-start;
        }
        
        .step-number {
            display: inline-block;
            width: 25px;
            height: 25px;
            line-height: 25px;
            text-align: center;
            background-color: #007bff;
            color: white;
            border-radius: 50%;
            margin-right: 10px;
            flex-shrink: 0;
        }
        
        .footer-links {
            margin-top: 30px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="success-container text-center">
            <div class="success-icon">
                <i class="bi bi-check-circle-fill"></i>
            </div>
            
            <h1 class="mb-4">提货成功！</h1>
            
            <% if (locals.game) { %>
                <div class="game-info">
                    <h4><%= game.name %></h4>
                    <img src="<%= game.imageUrl %>" alt="<%= game.name %>" class="game-image">
                </div>
            <% } %>
            
            <div class="alert alert-success">
                您的Steam礼物已发送成功！请查收并接受礼物。
            </div>
            
            <div class="steps-container">
                <h5>后续步骤：</h5>
                <div class="step">
                    <span class="step-number">1</span>
                    <div>登录Steam账户，您会收到一条好友消息</div>
                </div>
                <div class="step">
                    <span class="step-number">2</span>
                    <div>接受礼物邀请</div>
                </div>
                <div class="step">
                    <span class="step-number">3</span>
                    <div>游戏将添加到您的Steam库中</div>
                </div>
            </div>
            
            <% if (locals.redemptionRecord && redemptionRecord.id) { %>
                <div class="mt-3">
                    <p>提货记录ID: <span class="text-muted"><%= redemptionRecord.id %></span></p>
                </div>
            <% } %>
            
            <div class="footer-links">
                <a href="/" class="btn btn-primary">返回首页</a>
                <% if (locals.steamCommunityUrl) { %>
                    <a href="<%= steamCommunityUrl %>" class="btn btn-outline-secondary ms-2" target="_blank">
                        打开Steam
                    </a>
                <% } %>
            </div>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css"></script>
</body>
</html>
